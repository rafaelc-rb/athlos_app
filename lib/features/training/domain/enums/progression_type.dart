/// What to increment when a progression rule triggers.
enum ProgressionType {
  /// Add weight (kg).
  incrementWeight,

  /// Add reps per set.
  incrementReps,

  /// Add an extra set.
  incrementSets,
}
